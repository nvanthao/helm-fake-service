apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fake-service.fullname" . }}-test-connection"
  labels:
    {{- include "fake-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fake-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
